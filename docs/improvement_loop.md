# 改善ループ（AI駆動のブラッシュアップ機構）

AI（Claude Code）がカブログを継続的にブラッシュアップし、**方向性の判断は人間（あなた）が握る**ための機構。
本ドキュメントが**バックログの正本**であり、ループの**プロトコルの正本**。

設計思想は `docs/ai_driven_development.md` の延長。あの文書が「AIに実装させる側（インバウンド）」の
harness なら、本文書は「改善サイクルそのものを回す側（アウトバウンド）」の harness。
改善の駆動を規約でなく**構造（バックログ＋ゲート＋起動）**に落とす。

---

## 1. ループの形

```
①観測 → ②提案（候補+根拠+判断軸照合） → 〔③方向性ゲート＝あなた〕 → ④実行 → ⑤検証 → ⑥学習
   ↑                                                                              │
   └──────────────────────── 学習が次の観測を賢くする ──────────────────────────┘
```

各段の実体（大半は既存資産を使う。新規は②のバックログと③のゲートのみ）：

| 段 | すること | 使うもの |
|---|---|---|
| ①観測 | 改善の種を拾う（後述の3信号源） | `product_context.md` / `uiux_review.md` / `run-app` skill |
| ②提案 | 候補を並べ、根拠と判断軸照合を付ける | **本ファイルのバックログ表** |
| ③**ゲート** | あなたが go / 見送り / 方向修正 | **`/improve` の AskUserQuestion** |
| ④実行 | 実装する | Claude Code + agents + skills |
| ⑤検証 | AI が自分で確かめる | `pytest` / `verify` / `run-app` / `security-review` |
| ⑥学習 | 何が効いたか残す | 本ファイル §5 学習記録 / memory / doc 改訂ノート |

---

## 2. この機構の3つの決定（2026-07 確定）

方向性はあなたが決めた。以下は動かさない前提。変えるときは明示的に。

### 決定A：ゲートは「提案の前」に置く（AIの自律度）
**AI はコードを勝手に触らない。** ①観測→②提案（バックログへの候補追加）までを自走し、
あなたが候補に go/no-go を付けて**初めて④実行が走る**。
- 実装・migration・削除・リファクタは、バックログで `承認` になった項目のみ着手する。
- 未承認（`候補`）の項目を先走って実装してはならない。これが本ループの唯一の不変条件。

### 決定B：信号源は3つ（何を直すか、の起点）
①観測は次の3つだけを起点にする（コード健全性・技術的負債は本ループの起点から**外す**。
それらは `ai_driven_development.md §7 残課題` で別管理）。

1. **判断軸との差分** — `product_context.md` の問い
   「これは投資思考の**記録・蓄積・振り返り**に直接貢献するか？」に照らし、
   現状の画面・機能の穴（貢献できていない所、足せる所、引くべき所）を洗う。
2. **UI/UXレビュー** — `docs/uiux_review.md` の未対応項目＋初回体験（最初の5分）の穴。
3. **使用感（ドッグフーディング）** — あなたが実際に使って感じた摩擦、および
   `run-app` skill で AI が実機を触って見つけた引っかかり。

### 決定C：起動は「手動セッション起点」
**あなたが会話を始めた時だけ**ループを1周回す（`/improve` で起動）。
定期実行（cron/schedule）はしない。型が固まるまで手動で回し、過剰自動化を避ける。
自動化への移行は §5 学習記録が十分たまってから、別途あなたの判断で。

### 決定B-拡張（2026-07・R5）：コード健全性軸を追加
R1–R4 で「プロダクト機能をAI単独で探す」3信号源が doc-drift（実装済みの再発見）に
収束することが確定したため、ユーザー判断で **④コード健全性軸**（巨大ファイル分割・
死にコード除去・技術的負債）を起点に追加。この軸は「客観的・意図の誤読が起きない・
投機的でない」候補が出る唯一の領域で、AI駆動開発の速度・編集安全性に直結する。

---

## 3. バックログ（正本）

改善候補の唯一の正本。AI が観測で候補を追記し、あなたがゲートで `状態` を更新する。

**状態の遷移：** `候補`（未承認）→ 〔ゲート〕→ `承認` / `見送り` / `保留` → `実装中` → `検証` → `完了`

**判断軸照合：** 各候補は `product_context.md` の問いに対する ✅（貢献する）/ ❌（外れる）と
一言の根拠を必須にする。❌ は原則バックログに載せない（載せるなら理由を明記）。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| L1 | 登録後の自動ログイン（最初の5分の摩擦除去） | UI/UX・初回体験 | ✅ 記録を始める障壁を下げる | 完了 | R1で確認：`settings.py` `ACCOUNT_EMAIL_VERIFICATION='optional'`→allauth が登録後そのままログイン（実装済み） |
| L2 | ホーム空状態の3ステップ・オンボーディングカード | UI/UX・初回体験 | ✅ 蓄積の最初の一歩を導く | 完了 | R1で確認：`home.html:319`→`onboarding_card.html`（記録→つながり→振り返り）実装済み |
| L3 | タイムライン／ダッシュボードの空状態リンク | UI/UX・初回体験 | ✅ 振り返り導線を空状態から繋ぐ | 完了 | R1で確認：`timeline.html:221`・`trading_dashboard.html:343` に CTA リンクあり（実装済み） |

> L1–L3 は初回シード（`improvement_plan.md` の未確定項目由来）。ラウンド1（2026-07-04）で
> 実コードを確認し、いずれも既に実装済みと判明 → `完了`。

**R1 ドッグフーディング（run-app + Chrome/Playwright 実機観測）で追加した候補：**

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| N1 | ホームのカード抜粋が生Markdown（`##`・`-`）を表示 → `extract_lead` に統一 | 使用感 | — | 恒久見送り（意図的仕様） | **これはバグではなく意図的な設計判断。** 一覧でMarkdownを描画するとヘッダーが大きく映り、カードごとに文字サイズが変わって視線が疲れる。それを避けるため home のカード抜粋（`diary_card.html:110-112`）は`reason\|linebreaks`で**あえて生表示**している。timeline との差は不整合ではなく意図。今後この統一・除去を再提案しないこと |
| N2 | FAB(+)がカード本文に重なる（home/timeline） | 使用感 | ✅ 記録の読字を妨げない | 見送り | R2でユーザー判断：今後も実装しない。下部UI混雑は `uiux_review.md §5-3` で既知の許容範囲 |
| N3 | create の初回ツアー再表示条件の確認（毎回か初回のみか） | 使用感 | ✅ 記録開始の摩擦を下げる | 見送り | R2調査：`feature-tour.js` は `localStorage['tour-completed:diary-create-v1']` で完了後は再表示しない（`isCompleted`で早期return）。実機で出たのはテストブラウザのlocalStorageが空だったため＝正常動作。修正不要 |
| T1 | 仮説キャプチャの「賭け化」再設計（確認の目印を構造化・claim自動生成・次決算既定・自由記述の逃げ道） | 実ユーザー検証 | ✅ 検証（答え合わせ）が回らない核を解く＝振り返りの最深部を起動する | 完了（検証済み） | **実ユーザー検証で発見した本命候補を実装完遂。** `Thesis.checkpoint`/`checkpoint_direction` 追加（migration 0021）・claim自動生成・horizon既定=next_earnings・worst_case前出し・basis畳み込み・検証時の目印問い返し。設計 `docs/thesis_capture_redesign.md`、テスト `tests/test_thesis_checkpoint.py`。pytest 816 green・run-app 実機3画面確認 |
| CH1 | detail.html（2939行）の分割＝巨大テンプレの整理 | コード健全性 | ✅ AIの編集安全性・コンテキスト効率（AI駆動開発の速度に直結） | 完了（検証済み） | R5：5タブを `partials/detail/_tab_*.html` に機械抽出（挙動不変）。2939→2479行。各partialに `{% load %}` を付与（includeは親のloadを継承しない罠に対応）。pytest 821 green・run-app 全タブ描画確認。※docの§7「views.py 3600行」→実1819・§2.2「comparison死にコード」→301リダイレクトに整理済み、と判明（doc-drift） |
| D1 | 陳腐化ドキュメントの実装同期（false候補の抑制） | 判断軸／harness | ✅ 改善ループの観測精度を上げる（誤検出を減らす） | 完了 | R3で実施：`growth_os_redesign.md` 8e「予定」→`実装済`（django-q配線を明記）、`improvement_plan.md §初回体験`「状態不明/確認が必要」→`実装済`（L1-L3の根拠付き）に同期。doc-only・コード無変更 |
| G1 | 日記関連グラフ：検索したら目的の銘柄へ直接飛ぶ（検索→自動フォーカス/ズーム） | 使用感（R6） | ✅ 蓄積した記録を探しやすくする（判断軸ど真ん中） | 完了（検証済み） | R6実装：`_applySearch` を純粋なまま保ち、検索入力ハンドラから `_maybeAutoFocusSearch` を呼び、単一一致→`_enterFocusMode`＋`_fitToView`（近傍2ホップへズーム）。クリアで自動フォーカス解除。populatedシード（`create_realistic_test_data`）でrun-app検証：「東京エレクトロン」検索→6ノードに絞込＋ズーム。「密集して探せない/増えるほど確認しにくい」を構造から解消 |
| G3 | 日記関連グラフ：エッジの曲線化（ラインの重なり低減） | 使用感（R6） | ✅ 振り返りの地図を読みやすくする | 完了（検証済み） | R6実装：リンク `<line>`→`<path>` の弧（Q曲線・法線方向に距離の12%膨らませる）。CSSは既に `fill:none`・クラスベースのため崩れなし。tick で `d` を計算。populatedシードで曲線描画を確認。全821 green |
| G5 | 日記関連グラフ：サイドパネル（未接続一覧等）の長い中身で行ごと縦に伸び、SVGが拡大・見切れる不具合 | 使用感（R6・バグ） | ✅ 振り返り画面の破綻を直す | 完了（検証済み） | R6ユーザー報告：未接続バッジ→パネル開く→空白クリックでパネル閉→SVGの縦幅が広がり見切れる。根因：`.graph-outer-wrapper`（flex行）に高さ拘束がなく `align-items:stretch` で背の高いパネルに引きずられていた。行高を `clamp(520px,74vh,900px)` に固定＋`min-height:0`、モバイル（縦積み）は `height:auto` に戻す。run-app検証：60件の長パネル注入でも行/SVG高さ不変（PASS）。※CSSレイアウト不具合のためpytest回帰は無く、run-app実測で担保 |
| G4 | 日記関連グラフ：タグノードの過密で銘柄が見えない → タグを控えめ＋葉タグ除去 | 使用感（R6） | ✅ 「要素で検索→関連銘柄を探す」workflowの視認性を上げる | 完了（検証済み） | R6実ユーザー洞察「軸では考えない。要素で検索し関連銘柄を探す。タグが出すぎて見にくい」。**当初1ホップ案は却下**（関連の連鎖＝グラフの価値が死ぬとユーザー指摘）。関係は保ちタグ"ノード"の圧だけ下げる方針に。T-B1: ハブ半径 56→34・`fill-opacity:0.55`（ハイライト時は戻す）で銘柄を主役化。T-B2: `_pruneLeafTags`（接続銘柄≤1のタグを除去＝関連を作らないノイズのみ、構造は不変）。populatedシードで銘柄が浮き上がるのを確認・全821 green |
| G2 | 日記関連グラフ：コントロールの整理（接続4モード等の過多を解消） | 使用感（R6） | ✅ 振り返りの入口を直感的にする | 候補（保留） | R6摩擦②。ユーザーは今回 A（フォーカス）＋B（曲線）を選択し G2 は保留。ヘッダー約12操作の整理は将来。seedで検証可・中リスク |

**R7 タグ親子関係の摩擦（`/improve tagsの親子関係について`・信号源③・本セッション）で追加した候補：**

> このセッションで、ユーザーが「半導体という要素 → 関わる銘柄」を関連グラフで辿るために
> タグの親子（`@半導体`→`@イメージセンサー` 等）を検討 → **「接続がかなり紛らわしくなり運用できない」**
> と結論し、フラット構成へ revert（tag_master.md / seed_master_tags は git clean 済み）。
> 観測で判明：**親子タグは「語彙の整理」と「グラフの接続」で役割が分かれ、摩擦は後者だけに限局**。
> 語彙の親子（タグ一覧の階層・`@AI`/`@フィジカルAI`・`@エネルギー`/`@LNG` 等の tag_master 細分）は
> 正当に使用中で混乱源ではない。混乱は関連グラフの `tag_hierarchy`（ハブ↔ハブ）エッジのみ。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| TP1 | 関連グラフの**タグ親子エッジ(`tag_hierarchy`)を撤去**し「銘柄↔タグ」のフラット接続に統一。語彙の親子（タグ一覧の階層・tag_master 細分）は温存 | 使用感（R7） | ✅ 振り返りの地図の接続を読みやすくする＝ユーザーが明言した摩擦を直接解消 | 完了（検証済み） | R7実装：`utils.py get_tag_graph_data` から `tag_hierarchy` エッジ生成と `parent_pk` ノード属性を除去／`api_views.py` の tag_hierarchy 分岐を撤去／`diary-graph.js`（色・凡例）と `tag_detail.html`（色・破線）の死んだ分岐を除去。**語彙側（`tags/views.py` 階層UI・`TagForm` parent・`seed_master_tags`・`tag_master.md`）は不変**。既存テスト2件（親子エッジ有りを期待）を「親子でも tag_hierarchy は出ない＋フラット接続は残る」の回帰ガードに書換え。pytest **827 green**、実HTTP経路（`Client`＋`force_login`＋実シード）で tag 12件・tag_hierarchy 0件・parent_pk なし=PASS |
| TP2 | 親子エッジを撤去せず**既定OFF＋トグル表示**に（関係情報は残しつつ既定の混乱を消す） | 使用感（R7） | ✅ 同上（ただし混乱源を既定表示に残すぶん弱い） | 見送り | TP1 採用により不要（撤去が最短で摩擦を消す）。トグル追加は G2「操作過多」と逆行 |

**R8（`/improve tagsにこの軸の既存タグの子タグにする機能は使い道があるか`・信号源③）で追加した候補：**

> R7 で関連グラフから親子エッジを撤去した結果、per-user `Tag.parent` の**現在の消費先は
> タグ一覧の階層表示ただ1つ**に。しかも `@タグ` 記録では parent は自動付与されず（`_sync_hashtag_tags`
> は axis のみ）、ユーザーが `TagForm` で能動的に親を選んだ時だけ付く＝通常は休眠。語彙の階層は
> `MasterTag`/tag_master 側にあり per-user Tag には流れないため、一覧の階層表示は同じ階層の手作業の
> 二重管理になる。グラフ・関連度への寄与はゼロ（`utils.py:956` の `select_related('parent')` も死んだ最適化）。
> ＝ユーザーが「紛らわしい」と却下した当の機能が foot-gun として残存。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| TP3 | ~~per-user 親子タグ編集をユーザーUIから撤去~~ | 使用感（R8） | — | 見送り（撤回） | **実装途中で TG-DD の方向転換により撤回・全ロールバック（`git checkout`）。** ユーザーがドリルダウン再設計で「小要素＝宣言（親子タグ）」を選択したため、親子タグ編集は**温存が必要**になった（TP3 は前提が消えた）。親子は「フラットグラフの混乱源」から「ドリルダウンの階層ソース」へ役割が反転 |

**R9（`/improve 関連グラフを根本から作り替え`・信号源③）で追加した候補：**

> R7/R8 で「親子は紛らわしい→フラット化」と進めた矢先、ユーザーがより上位の課題を提示：
> **関連グラフの相互作用モデルそのものを作り替えたい**。課題＝(1)全件表示に意味がない・
> (2)ノード過多で見たい要素が埋もれる。欲しい体験＝**空から要素を1つ選び、ドリルダウンで
> 深掘り**（半導体→小要素[AI半導体・半導体装置]→関連銘柄→銘柄の別要素…）。タップで隣接だけ広がり、
> 他は畳んだまま。**重要な反転：親子は「一度に全部見せる」と紛らわしいだけで、"畳んで開く"
> progressive disclosure なら階層はむしろ価値になる**。ゲート（AskUserQuestion）で確定した設計方針：
> ①小要素の出どころ＝**宣言（親子タグ）**（→ TP3 撤回・親子編集を温存）、②初期表示＝**空から選択で開始**
> （選択→隣接のみ展開、タップで深掘り、他は畳む）。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| DL1 | **explore（要素で探索）への導線を追加**：現状どこからもリンクが無く直打ち専用。`base.html` のナビ（デスクトップ `nav-item`＋モバイル `menu-item`）の「関連グラフ」の隣に「要素で探索」を追加 | 使用感（R10・実使用で好評→到達不能が唯一の穴） | ✅ 想起の主機能を実際に使える状態にする（発見性＝機能の生死） | 完了（検証済み） | ゲートで「ナビのみ」を選択。`base.html` の2箇所（デスクトップ `nav-item`・モバイル `menu-item primary`）に「要素で探索」（`bi-diagram-3-fill`／関連グラフ=線・探索=塗りで区別）を追加。実HTTP経路で home 200・ナビ2箇所出現・`/explore/graph/` リンク解決を確認。pytest 838 green。橋渡しリンク（diary_graph/tag_detail）は今回見送り |
| DF1 | **タグ `df`（出現銘柄数）の鮮度ギャップを塞ぐ**：①日記削除時に当該タグの df を再計算（現状 `StockDiaryDeleteView.delete` は未実施＝削除後 df が過大のまま）②全タグ df を棚卸す再計算コマンド（安全網）。回帰テスト付き | 使用感（ユーザー懸念：タグは各ユーザーが自由に増減する） | ✅ タグ関連度（想起の精度）を正しく保つ。誤関連は作らないが削除後スコアが弱く出る | 候補 | 影響は限定的（IDF は `utils.py:1111` で保存 df に依存・方向は"弱めに出る"だけ・次のタグ使用で自己修復・`get_tag_graph_data` はライブ集計で無傷）。df 同期の主経路（`_sync_hashtag_tags`＝@タグ保存時）は健全。ユーザー分離・改名/軸変更は対応済み。実装小 |
| TG-DD | **関連グラフをドリルダウン型に再設計**：初期は空（要素選択/検索）→選んだ要素＋隣接だけ展開→タップで銘柄・子要素・別要素へ深掘り。階層ソースは宣言済み親子タグ（`Tag.parent`） | 使用感（R9） | ✅ 「テーマ→関連銘柄→別テーマ」の探索を成立させ、蓄積した記録の想起を主機能化（判断軸ど真ん中） | 実装完了（実機スクショ残） | 設計ファースト（`docs/graph_drilldown_redesign.md`）→§8 5論点をユーザー確定（オンデマンド近傍API・検索のみ・再タップで子孫も畳む・銘柄は展開＋詳細導線・ノイズ除外は現行踏襲）→実装。専用新ビュー `/stockdiary/explore/graph/`（既存 diary_graph 不変）。近傍導出 `utils.get_graph_neighbors`（純粋関数）・API `graph_neighbors`・新テンプレ・新JS `explore-graph.js`。**pytest 838 green**、実HTTP経路（force_login＋実シード）で タグ展開＝子タグ＋銘柄／銘柄展開＝タグ＋detail_url を確認。ブラウザ実機スクショは run-app が web サンドボックス専用のため未（要ユーザー環境 or 別途） |

**アプリ採点レビュー（2026-07-05・信号源①判断軸＋④コード健全性）で追加した候補：**

> アプリ全体の採点（A-/86点）で「凍結中の最重量物」として浮上。`investment_hub.html` は
> **2,576行＝リポジトリ最大のテンプレート**のまま凍結されており（uiux_review「2026-06 は
> 触らない」）、去就の判断材料を棚卸した。機能インベントリと判断軸照合：
>
> | hub 内セクション | 中身 | 判断軸照合 / 代替 |
> |---|---|---|
> | 銘柄検索・比較選択 | 最大5銘柄を選ぶ | （下記の各機能の入口） |
> | 財務比較・CAGR 比較 | `get_stock_historical` で外部データをオンザフライ比較 | ❌ 記録・蓄積・振り返りに寄与しない（証券アプリ/スクリーナーの領分） |
> | 信用残チャート | margin データの比較表示 | detail の margin 表示・分析API `margin` フィールドが本体側に既存 |
> | 買いシグナル | テクニカル系表示 | ❌ 売買判断は product_context の対象外 |
> | AI見解（Gemini比較） | `api_gemini_stock_analysis` | **SuperUser 限定＝一般ユーザーには死に機能**。Claude 分析API連携が上位互換 |
>
> 唯一の理論上の価値は「比較しながら考えたことを記録に落とす」だが、現状 hub に記録への
> 導線は無い。旧 comparison（1,071行）は削除＋恒久リダイレクトの前例あり（uiux_review 2.2）。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| IH1 | ~~investment_hub の撤去~~ | 判断軸＋コード健全性（採点レビュー） | — | **見送り（ユーザー判断）** | ゲートで「**投資判断サポートは必要**」と明示されたため存続（メニュー露出も維持）。今後も撤去を再提案しないこと。凍結（UI改修対象外）の扱いは従来どおり |

**改修後ドッグフーディング（2026-07-05・信号源③ run-app 実機ウォークスルー）で追加した候補：**

> 改修5フェーズ完了後、モバイル375pxで主要フローを実測。**良好と確認できた点（再提案不要）**：
> 継続記録はカードタップ→FAB→記録追加の3タップで到達し必須入力は本文のみ／クイック記録は
> FAB→シートの2タップ＋2入力／取引追加は5フィールドで適正／timeline に FAB あり（3.2実施済み）／
> モバイルにも通知ベルあり／home ファーストビューに最初の日記カードが入る（637px/812px）／
> 検索サジェスト動作。**摩擦として観測されたのは以下**。ナビは決定10「主要5項目」に対し
> 実測14項目（主要7＝日記一覧/タイムライン/要素で探索/関連グラフ/ダッシュボード/決算カレンダー/
> 新規作成 ＋ 設定その他7）に再肥大しており、スマホメニューは1画面に収まらない。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| NV1 | **ナビ再肥大（14項目）の整理**：①「関連グラフ」をナビから外す（explore 内「全体マップで見る→」リンクに一本化。TG-DD 後は explore が主・全体マップが従）②「決算カレンダー」を設定・その他へ降格 ③ IH1 採用時は「投資判断サポート」も消える → 主要5（日記一覧/タイムライン/要素で探索/ダッシュボード/新規作成）＋その他6へ | 使用感（実測14項目・メニュー1画面超え） | ✅ 決定10「主要5項目」の回復。2つのグラフ並存の混乱も同時に解消 | 完了（検証済み） | ゲート承認（③は IH1 見送りにより**投資判断サポート存続**へ修正）。`base.html` のPCナビから関連グラフ・決算カレンダーを撤去、メニュー主要セクションも同様、決算カレンダーは「設定・その他」へ移設 → 主要5（日記一覧/タイムライン/要素で探索/ダッシュボード/新規作成）。※複数行 `{# #}` コメントが本文へ漏れる既知の罠を実機で検出し `{% comment %}` で回避（回帰テストに追加）。pytest 866 green・実機スクショ確認 |
| TL1 | **タイムラインにキーワード検索を追加**：period/type/tag フィルタは実装済みだが自由語検索が無く、「あのとき何を考えていたか」を言葉で探すには home へ戻るしかない（時系列文脈が失われる） | 使用感（実機で確認） | ✅ 蓄積した記録を探しやすくする（判断軸ど真ん中） | 完了（検証済み） | `?q=` を追加（銘柄名・ノート本文/トピック・取引メモ・日記reasonを横断）。既存チップ/タグselectのURLにも q を引き継ぎ。回帰テスト4件（`tests/test_timeline.py::TestTimelineKeywordSearch`） |
| DK1 | **ダッシュボードと投資家カルテの役割分裂を橋渡し**：dashboard＝お金の成績（損益/勝率/業種ROI）、karte＝判断の質（的中/象限）で「成績を見る場所が2つ」。論点5「汎用統計→判断の質へ再構成」が中途の状態 | 使用感＋uiux_review 論点5 | ✅ 振り返りの入口の迷いを減らす | 完了（第1段階） | 両ページ冒頭に役割ラベル＋相互リンクを設置（dashboard「ここはお金の成績→カルテへ」/ karte「お金の成績はダッシュボードへ」）。統合の判断は利用実態を見て別途ゲート。回帰テスト2件 |
| FB2 | **home FAB の純化**：現状4項目（クイック記録/新規登録/テンプレート/タグ管理）のうち管理系2つ（テンプレート/タグ管理）はメニューと重複。FAB を記録動線2つに絞る | 使用感（記録と管理の混在） | ✅ 記録の最短動線を濁らせない | 完了（検証済み） | home の `form_actions` をクイック記録/新規登録の2つに純化。編集ページのFAB（戻る/テンプレ/タグ）は承認スコープ外のため不変。回帰テストで labels を固定 |
| LB1 | **メニュー「データ移行」の改名**：エクスポート機能（CSV）の在り処がこの名前から分からない。「エクスポート / インポート」等へ | 使用感（発見性） | ✅ 「記録が資産」＝持ち出せることの発見性 | 完了（検証済み） | メニューラベルを「エクスポート / インポート」へ変更。回帰テストで「データ移行」の不在を固定 |

**「繰り返し課題」構造スキャン（2026-07-05・信号源④コード健全性）で追加した候補：**

> 「同じ種類の事故が繰り返し起きる構造」を機械走査で棚卸し。走査した9クラスのうち
> 2クラスは**即日、構造ガード（`tests/test_template_lint.py`）に変換済み**：
> ①複数行 `{# #}` コメントの本文漏れ（過去3回＋今回ラウンドでも2回発生→全テンプレ走査で禁止）
> ②base.html と個別ページの同一JS二重読み込み（const再宣言 SyntaxError の実績→全テンプレ走査で禁止）。
> 以下は判断が要るため候補として残す。

| # | 候補 | 信号源 | 判断軸照合 | 状態 | メモ |
|---|------|--------|-----------|------|------|
| CH2 | **STATIC_VERSION と sw.js VERSION の二重手動管理を解消**：現在 `config/settings.py:852` と `static/sw.js:2` を人手で同時更新する運用（今セッションでも2回手動同期が発生）。片方の更新漏れ＝古いキャッシュ配信事故になる | コード健全性（運用で実測） | ✅ 更新漏れによる「直したのに直らない」を防ぐ | 候補 | 案: (a)整合性チェックテスト（両者一致を pytest で強制・最小） (b)`sw.js` をテンプレート配信にして settings から注入（大きめ）。まず (a) が費用対効果最良 |
| CH3 | **ナビの二重定義を単一ソース化**：PCヘッダー（`nav-item`×5）とモバイルメニュー（`menu-item primary`×5）が base.html 内の別々の手書きリスト。今回の NV1 でも2箇所同時編集が必要だった＝将来のナビ変更で片側だけ直る drift が構造的に起きる | コード健全性（NV1実装で実測） | ✅ ナビ再肥大・片側drift の再発防止 | 候補 | 案: ナビ項目を1つのリスト（テンプレ変数 or include）に定義し両方を描画。表示形式が異なるためテンプレ側は2つのままデータだけ共通化する |
| CH4 | **diary-detail-page.js のハードコードURL集約**：`/stockdiary/...` 直書きが6箇所（EDINETパネル・関連日記リンク・株価メトリクス等。インライン時代からの持ち込み）。URL変更やプレフィックス変更で静かに壊れる | コード健全性 | ✅ 既存の `DIARY_DETAIL_CONFIG.urls` パターンに揃えるだけ | 候補 | Phase1 外部化で「テンプレ変数由来」は config 化済みだが、元から直書きだった分が残存。同パターンへの追従で解消。実装小 |
| CH5 | **`except Exception: pass` の握りつぶし19箇所の選別**：stockdiary/common に無言の握りつぶしが19箇所。yfinance 系の「欠損は仕様」箇所は妥当だが、バグを隠すものが混在していないか選別し、最低限 `logger.debug` を入れる | コード健全性 | ✅ 「静かに壊れる」の温床を減らす | 候補 | 一括変換はせず、外部データ欠損（妥当）とロジック例外（要ログ）を1箇所ずつ選別する。Sentry 導入（P1提案）とセットだと効果大 |
| CH6 | **timeline イベント色の三重定義を一本化**：同じ4色が `views_timeline.KIND_UI`・`timeline.html` のCSS変数・種類チップの inline style の3箇所に直書き。色変更で3箇所同時修正が必要 | コード健全性 | ✅ 振り返り画面の見た目変更を安全にする | 候補 | CSS変数（`--tl-k-*`）に一本化し、KIND_UI は色を持たずクラス名を持つ形へ。実装小 |

---

## 4. `/improve` ラウンドの手順（プロトコル）

`/improve` を起動すると AI は1周を回す。詳細な起動手順は `.claude/commands/improve.md`。

1. **観測**：3信号源から候補を集める（判断軸チェック・`uiux_review.md`・必要なら `run-app`）。
   既存バックログの `候補` も棚卸しする。
2. **提案**：新候補を §3 バックログに `候補` で追記（判断軸照合を必ず付ける）。
3. **ゲート**：`AskUserQuestion` で、今回のラウンドで着手する候補をあなたに選ばせる
   （go / 見送り / 方向修正）。**ここで承認された項目だけ**が次に進む。
4. **実行**：承認項目を実装（既存規約・`AggregateService.deferred` 等の不変条件を守る）。
5. **検証**：`pytest` green・必要に応じ `run-app`/`verify`/`security-review`。バグ修正時はテスト追加（CLAUDE.md 規約）。
6. **学習**：結果と学びを §5 に1行記録。バックログの状態を更新。

---

## 5. 学習記録（⑥）

各ラウンドで「何を承認し／何が効き／何を見送ったか・なぜ」を1行で残す。
これが次の観測を賢くし、将来の自動化判断の材料になる。

| ラウンド日 | 承認・実行した項目 | 結果／学び | 見送り・保留と理由 |
|-----------|------------------|-----------|-------------------|
| 2026-07-04 (R1) | run-app ドッグフーディング（観測のみ・実装なし） | シード L1–L3 は全て実装済みと判明（`improvement_plan.md` の「状態不明」は古かった）→ `完了`。ループが最初に炙り出したのは「バックログの陳腐化」だった。実機観測で新候補 N1–N3 を発掘（N1=ホーム抜粋の生Markdown、コードで裏取り済み） | 実装は今回見送り（あなたの判断）。N1–N3 は `候補` のまま残置し R2 以降で再度ゲートにかける |
| 2026-07-04 (R2) | 棚卸しのみ（実装なし） | N3 は正常動作と判明（`feature-tour.js` が初回のみ表示）→ `見送り`。**N1 は「バグ」ではなく意図的仕様だった**——一覧でMarkdownを描画するとヘッダーが大きく映り文字サイズがバラついて視線が疲れるため、home抜粋はあえて生表示している（ユーザー指摘）。N2 も恒久見送り。**最大の学び：AIが「不整合＝バグ」と読んだものが、実は意図ある設計だった。既存挙動を"直す"前に、それが意図的でないかをまず疑う**（→ memory [[home-list-raw-markdown-intentional]]） | N1・N2・N3 すべて `見送り`。可読性の細部は深追いしない。次ラウンドは判断軸により本質的な穴（記録・蓄積・振り返りの体験）を起点にする |
| 2026-07-04 (R3) | D1：陳腐化doc 2件を実装同期（doc-only・コード無変更） | 振り返りの核（成長OS 8a–8e）まで踏み込むも全て実装済み。**3ラウンド通しての本質パターン：AI単独の観測は「プロダクトの穴」でなく「ドキュメントの陳腐化」を繰り返し検出する**（L1-L3「状態不明」→済、8e「予定」→済）。アプリは成熟し実装がdocを追い越している。だから残る本命の信号源は③使用感（ユーザーが感じる実摩擦）。D1でdocを実態同期し、doc-drift由来のfalse候補を抑制 | 生きたプロダクト候補は無し。**次ラウンド以降は AI の穴探しでなく、ユーザーの使用感を起点にする**（信号源③を主にする方向へ運用を寄せる） |
| 2026-07-04 (R4) | **T1 仮説キャプチャ賭け化を実装完遂**（実ユーザー検証→診断→設計ファースト→実装→検証） | **これが改善ループの完全な1周の初例。** 実ユーザー（naotaro）が検証を回さない実摩擦を起点に、AIの穴探しでは絶対出ない本質課題（④数値への接続）を特定。「④は数値のモデル化でなく"見張る目印を1つコミット"」に再定義し、幅の懸念（イベント/定性型）は目印一般化＋自由記述の逃げ道で解消。設計を先に文書化してから実装。**学び：本物の改善は実使用の摩擦からしか出ない／設計ファーストが誤実装を防ぎ、実装時に更に軽い解（トグル→claim任意化）へ収束できた** | 信号源③（使用感）が唯一の生きた鉱脈だと確定。以降も実運用の摩擦を起点にする |
| 2026-07-04 (R5) | CH1 detail.html 分割（コード健全性軸・新設）。仮説/検証の実運用検証は1サイクル回るまで保留 | 機能軸に加えコード健全性軸を追加。観測すると **documentedな負債もほぼ解決済み**（views.py 3600→1819・comparison死にコード→301リダイレクトに整理済み・htmlcov ignore済み）＝機能軸と同じ doc-drift パターン。唯一の実在負債＝巨大テンプレを1つ処理（detail.html 2939→2479、5タブをpartial化・挙動不変）。**学び：includeは親の `{% load %}` を継承しないため各partialにloadが要る（Djangoの罠）／機械抽出＋全タブ実機検証で無挙動変更リファクタを安全に回せた** | このコードベースは機能・健全性とも高完成度。次も実運用の摩擦を起点に |
| 2026-07-04 (R6) | 日記関連グラフの使いにくさ（ユーザー起点）→ G1検索自動フォーカス・G3エッジ曲線化・G4タグ控えめ＋葉タグ除去を実装 | **信号源③（実運用の摩擦）が最も効くことを再実証。** ユーザーが実グラフのスクショで摩擦を提示→原因を毎回裏取り→設計ファーストで合意→populatedシード（`create_realistic_test_data`）で実機検証。**最重要の学び：ユーザーとの対話が誤設計を何度も引き戻した**——(1)「軸フィルタを前に出す」提案は「軸では思考しない」で却下、(2)「1ホップに絞る」提案は「関連の連鎖＝グラフの価値が死ぬ」で却下→関係を保ちタグ"ノード"の圧だけ下げる正解へ。**AI単独なら関連性を壊す方向に実装していた。方向性ゲートを人間が握る設計の価値が最も鮮明に出たラウンド** | G2（コントロール整理）は保留。実運用で追加摩擦が出たら次ラウンド |
| 2026-07-05 (R12・ドッグフード) | **explore の初回体験ポリッシュ：E1 展開状態バッジ／E3 空状態の案内**（新規コードのドッグフード観測→ゲートで E1・E3 を選択、E2ファンアウトは見送り） | E1＝ノードに「+ 開ける／− 開いてる／印なし 葉」バッジを付け、タップ前に状態が分かるように（発見性）。E3＝タグ付き記録が0件のとき検索を無効化し「日記を作成 →」CTA付きの案内に切替。**学び：新規機能はドッグフード観測が doc-drift にならない数少ない生きた領域。ただしポリッシュは実使用で要否を確かめる前提で小さく出す**。JS/テンプレのみ・840 green・実HTTP経路で通常/空ユーザー両方 200 確認 | E2（ファンアウト段階化）は実使用で過密が問題化してから |
| 2026-07-05 (R11・方針決定) | **2つのグラフの位置づけを確定：explore 主役＋相互リンク＋関連グラフ廃止は保留** | ユーザーが「関連グラフ（全件俯瞰）と要素で探索（ドリルダウン）をどう扱うか」を提起。役割は「俯瞰 vs 深掘り」で別物。実ワークフロー（R6/R9＝要素→関連銘柄を辿る）は explore 側なので**explore を主役**に。ただし関連グラフ固有機能（業種・手動リンク・@メンション・方向・全体像）の使用実績が無いため**廃止/統合は保留**（不可逆判断を証拠が出るまで遅らせる）。統合は G2「操作過多」再燃で却下。実装：①ナビ並びを explore 優先②相互リンク（explore→「全体マップで見る」／diary_graph→「要素で探索」／tag_detail→「この要素で探索」）③explore を `?start=<node>` で起点自動オープン可に（タグ詳細から文脈を持って飛べる）。**学び：迷ったら"消す/統合"より"役割を分けて相互リンク"＋観測ポイント設定が低リスク**。pytest 840 green・実HTTP経路で全リンク解決確認 | 関連グラフの格下げ/廃止は「explore主運用で固有機能を実際に開くか」を観測してから次ラウンドで判断 |
| 2026-07-05 (R10d・調整) | **explore：タップ時にノードが激しく左右に揺れる問題を軽減** | 定番の force 調整：新規ノードを展開元の近くに生成（原点からの fling 防止）・既存ノードは速度ゼロで静止・`forceCenter`（重心再配置で全体が振れる）→弱い `forceX/Y` に置換・`velocityDecay` 0.4→0.55・リヒート alpha 0.6→0.35。JS のみ | 実機での体感確認はユーザー側（ブラウザ未接続のため） |
| 2026-07-05 (R10c・バグ修正) | **explore：生まれたノードを再タップすると全部畳む不具合を修正** | 根因＝グラフは無向で近傍リストに「親」も含まれ、旧 collapse が近傍の展開中ノードを再帰的に畳んだため親まで畳んでいた。修正＝`revealedBy`（展開元＝展開ツリーの親）を記録し、畳みは近傍でなく**展開ツリーの子孫だけ**を辿る。**学び：無向グラフで"子孫"を扱うなら別途 有向の展開ツリー（誰が誰を出したか）を持つ必要がある**。JSテスト基盤が無いため collapse ロジックを Node 単体再現で検証（seed とその近傍が残り、子ノードの展開ぶんだけ畳む）。pytest 840 green（JS のみ・Python 不変） | JS単体テスト基盤は未整備（既存 diary-graph.js も同様）。導入は過剰につき見送り、コードに「なぜ」をコメントで残す |
| 2026-07-05 (R10b) | **explore の起点を銘柄にも拡張**（ユーザー提案：タグ軸だけでなく銘柄からも開始可能に） | 近傍APIは元から `stock:` ノード対応済みだったため、**入口の埋め込みデータ＋JS検索に銘柄を足すだけ**で実現（銘柄はコード/名前で検索可）。**学び：良い基盤（近傍を汎用の node で表現）を敷いておくと、後の拡張が入口の差分だけで済む**。pytest 840 green・実HTTP経路で入口=13タグ+7銘柄・銘柄起点展開を確認 | — |
| 2026-07-05 (R10) | **DL1 explore への導線をナビに追加**（実使用で「すごく使いやすい」と好評→唯一の穴＝到達不能を解消） | 新機能は**発見性が無ければ存在しないのと同じ**。実装直後に導線を張るまでが1周。ナビ2箇所（デスクトップ/モバイル）に「要素で探索」を追加、アイコンの線/塗りで既存「関連グラフ」と役割を区別。テンプレのみ・home 200 とリンク解決を実HTTP確認・838 green | 橋渡しリンク（diary_graph/tag_detail から）は最小主義で見送り。必要なら次ラウンド |
| 2026-07-05 (R9) | **TG-DD 関連グラフをドリルダウン型に再設計（v1実装）**＋TP3 撤回 | ユーザーが R7/R8 の「親子→フラット化」の最中に上位課題を提示：全件表示に意味がなくノード過多で見たい要素が埋もれる→**空から1要素を選び深掘りする探索**が欲しい。**最重要の学び：直前に"混乱源"として撤去しかけた親子タグが、progressive disclosure（畳んで開く）に載せると探索の背骨に反転した**。同じ機能でも"一度に全部見せる"か"タップで1段ずつ"かで価値が真逆になる。設計ファースト（doc→§8の5論点をユーザー確定→実装）で、専用新ビュー（既存 diary_graph 不変）・オンデマンド近傍API・純粋関数で近傍導出。可視ノードを `expanded` 集合から再計算する設計で"再タップで子孫も畳む"を素直に実現。pytest 838 green・実HTTP経路で検証 | TP3（親子編集撤去）は前提が消え撤回・全ロールバック。TP1（フラット版の親子エッジ撤去）は別レイヤとして維持 |
| 2026-07-05 (R7) | **TP1 タグ親子エッジ(`tag_hierarchy`)を関連グラフから撤去**（`/improve tagsの親子関係について`・信号源③） | ユーザーが「半導体という要素→関わる銘柄」を辿るためタグ親子（`@半導体`→`@イメージセンサー`）を検討→**「接続がかなり紛らわしく運用できない」**と結論しフラットへ revert。**この摩擦を起点に観測して判明した核心：親子タグは「語彙の整理（タグ一覧の階層・`@AI`/`@フィジカルAI`・tag_master 細分）」と「関連グラフの接続（ハブ↔ハブの `tag_hierarchy` エッジ）」の2役割を兼ね、摩擦は後者だけに限局**。語彙側は正当に使用中で温存し、グラフの階層エッジだけ外科的に撤去（`utils.py`/`api_views.py`/`diary-graph.js`/`tag_detail.html`）。**学び：ユーザーが探索して却下した機能でも、"全撤去" ではなく "混乱源の役割だけ切る" が正解になることがある。役割を分解してから触ると温存すべき価値を壊さない**（→ memory [[tag-parent-child-graph-flat]]）。両消費者（関連グラフ・tag_detail ミニグラフ）が同一API経由と判明し1箇所の修正で両方フラット化 | TP2（トグル化）は TP1 採用で見送り。親子は "語彙" として残す方針 |

---

## 関連ドキュメント

- AI駆動開発の設計思想（インバウンドの harness）: `docs/ai_driven_development.md`
- プロダクトの判断軸（①観測の物差し）: `docs/product_context.md`
- UI/UXレビュー（②信号源）: `docs/uiux_review.md`
- 過去の論点管理（本ループの前身・人手運用）: `docs/improvement_plan.md`
- 起動トリガー: `.claude/commands/improve.md`
